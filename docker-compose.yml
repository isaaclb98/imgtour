services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - /path/to/images:/images:ro
      - ./export:/export:rw
    environment:
      - IMAGE_FOLDERS=/images
      - EXPORT_FOLDER=/export